import { useState } from 'react';
import { Link } from 'react-router-dom';
import { LocationFill, UnorderedListOutline, RightOutline } from 'antd-mobile-icons';
import { listMerchants } from '../services/merchant.service';
import { useCouponData } from '../hooks/useCouponData';
import Artwork from '../components/Artwork';
import MerchantCard from '../components/MerchantCard';
import Sheet from '../components/Sheet';
import DataState from '../components/DataState';
export default function MerchantsPage() {
  const state = useCouponData(listMerchants);
  const [mode, setMode] = useState('map');
  const [category, setCategory] = useState('餐饮');
  const [region, setRegion] = useState('建湖县');
  const [filter, setFilter] = useState('');
  const [selected, setSelected] = useState(null);
  const [search, setSearch] = useState('');
  const rows = state.data?.filter((item) => (category === '全部' || item.category === category) && (region === '全城' || item.region === region) && item.name.includes(search));
  return <div className="cv-merchants"><div className="cv-merchants-hero"><Artwork name="merchants-banner" alt="盐城惠民消费季，适用商家，好味好店，惠享盐城" /></div><div className="cv-map-tabs"><button aria-pressed={mode === 'map'} onClick={() => setMode('map')}><LocationFill />地图</button><button aria-pressed={mode === 'list'} onClick={() => setMode('list')}><UnorderedListOutline />列表</button></div><div className="cv-map-filters"><button onClick={() => { setRegion('全城'); }}>全城 <svg className="cv-filter-chevron" viewBox="0 0 16 16" fill="none" aria-hidden="true"><path d="m4 6 4 4 4-4" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round" /></svg></button><button onClick={() => setFilter('区域')}>{region} <svg className="cv-filter-chevron" viewBox="0 0 16 16" fill="none" aria-hidden="true"><path d="m4 6 4 4 4-4" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round" /></svg></button><button onClick={() => setFilter('行业')}>{category} <svg className="cv-filter-chevron" viewBox="0 0 16 16" fill="none" aria-hidden="true"><path d="m4 6 4 4 4-4" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round" /></svg></button></div>{mode === 'map' ? <div className="cv-map"><Artwork name="merchants-map-interactive" alt="建湖县演示商家分布示意图" />{rows?.filter((store) => Number.isFinite(store.mapPosition?.x) && Number.isFinite(store.mapPosition?.y)).map((store) => <button key={store.id} className="cv-map-marker" style={{ left: `${store.mapPosition.x}%`, top: `${store.mapPosition.y}%` }} aria-label={`查看${store.name}地图点位`} onClick={() => setSelected(store)}><svg viewBox="0 0 40 54" aria-hidden="true"><path d="M20 2C10 2 2 10 2 20c0 13 18 31 18 31s18-18 18-31C38 10 30 2 20 2Z" fill="#d77a50" stroke="#fffdf8" strokeWidth="2.5"/><path d="M13 12v8m4-8v8m-8-8v5c0 4 8 4 8 0m-4 4v13m13-22c-4 4-4 11 0 11v11m0-22v11" fill="none" stroke="#fffdf8" strokeWidth="1.8" strokeLinecap="round"/></svg></button>)}<button className="cv-map-locate" aria-label="查看附近商家" onClick={() => document.getElementById('coupon-nearby')?.scrollIntoView({ behavior: 'smooth' })}><svg width="24" height="24" viewBox="0 0 24 24" fill="none" aria-hidden="true"><circle cx="12" cy="12" r="7" stroke="currentColor" strokeWidth="1.8"/><circle cx="12" cy="12" r="3.3" fill="currentColor"/><path d="M12 2v3m0 14v3M2 12h3m14 0h3" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round"/></svg></button></div> : <div className="cv-page-padding"><input className="cv-search" aria-label="搜索商家" placeholder="搜索商家名称" value={search} onChange={(event) => setSearch(event.target.value)} /></div>}<section className="cv-nearby" id="coupon-nearby"><div className="cv-sheet-handle" /><h2>附近商家 <span>{rows?.length || 0}家</span></h2><DataState {...state} />{rows?.slice(0, mode === 'map' ? 2 : undefined).map((store) => <MerchantCard key={store.id} store={store} onNavigate={setSelected} />)}{rows?.length === 0 && <p className="cv-empty">暂无符合条件的商家</p>}{mode === 'map' && <button className="cv-text-button" onClick={() => setMode('list')}>查看全部{rows?.length || 0}家商家 <RightOutline /></button>}</section>{filter && <Sheet title={`选择${filter}`} onClose={() => setFilter('')}>{(filter === '区域' ? ['全城', '建湖县', '盐城市'] : ['全部', '餐饮', '商超', '文旅', '家电']).map((item) => <button className="cv-sheet-option" key={item} onClick={() => { if (filter === '区域') setRegion(item); else setCategory(item); setFilter(''); }}>{item}</button>)}</Sheet>}{selected && <Sheet title={selected.name} onClose={() => setSelected(null)} className="cv-merchant-sheet"><p>{selected.address}</p><p>营业时间：{selected.hours}</p><p>演示门店，仅用于体验消费券流程。</p><Link className="cv-primary" to={`/coupon/select/${selected.id}`}>选择此门店消费券</Link></Sheet>}</div>;
}
